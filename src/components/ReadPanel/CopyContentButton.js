// CopyContentButton.js
import React from "react";

const CopyContentButton = ({ handleCopyContent }) => {
  return <button onClick={handleCopyContent}>Copy Content</button>;
};

export default CopyContentButton;
